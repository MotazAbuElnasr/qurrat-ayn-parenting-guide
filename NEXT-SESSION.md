# برومبت الجلسة الجاية — إضافة اللهجة الخليجية

انسخ اللي بين السطرين ده وابعته كأول رسالة.

---

اقرا `CLAUDE.md` كامل الأول، وبعدين `build/build.js`، وبعدين افتح
`docs/content/sham.json` وشوف شكلها.

**المطلوب: إضافة اللهجة الخليجية (`gulf`) كرابع لهجة، كاملة زي الشامي — محتوى وواجهة.**

## الوضع الحالي

- `docs/index.html` (97KB) — قشرة وكود بس
- `docs/content/eg.json` — المصري، **الأصل اللي كل لهجة بتترجم منه**
- `docs/content/msa.json` · `docs/content/sham.json` — كاملين
- `src/index.js` فيه `/api/dialect` بيختار اللهجة من `cf.country`.
  الخليجي هيبقى الافتراضي في السعودية والكويت وقطر والبحرين وعُمان (الخريطة موجودة).
- `node build/build.js` بيتحقق من كل حزمة وبيختم اللهجات في القشرة وفي الـWorker.

## العدّة جاهزة — متبنيهاش من الأول

الشامي بُني بعدّة اتحطت في `build/dialect/`، وهي اللي وفّرت معظم الشغل:

| السكريبت | بيعمل إيه |
|---|---|
| `slice.mjs` | بيقص المصري لـ54 دفعة، أكبرها 34KB (حجم يقدر وكيل واحد يكتبه في ملف واحد) |
| `check.mjs` | **الأهم.** بيقارن كل دفعة بأصلها آليًا: عنصر ناقص، مفتاح اتغير، رقم اتبدّل، رابط اتكسر، حقل حرفي اتترجم، وسم HTML اتحرّك، `goVal` اتغير، backtick، تحديد مكان القارئ |
| `assemble.mjs` | بيركّب الحزمة: بيحط كل دفعة في مكانها، بينسخ القصص، **بيحسب علامات النافجيشن و`secGo` بالموضع**، وبيطبّق خريطة التسمية |
| `policy.mjs` | بيفحص الحزمة المجمّعة ضد قواعد CLAUDE.md + تسرّب المصري |
| `serve.mjs` | سيرفر محلي بيقدّم `docs/` وبيرد على `/api/dialect` (`wrangler dev` مكسور هنا) |

```bash
export WORK=/tmp/gulf                 # الدفعات تتولد برّا الريبو
node build/dialect/slice.mjs          # 54 دفعة في $WORK/in
# … الوكلاء بيكتبوا في $WORK/out …
node build/dialect/check.mjs          # لازم كلها ✓
node build/dialect/assemble.mjs gulf  # بيكتب docs/content/gulf.json
node build/dialect/policy.mjs gulf
node build/build.js
```

**قبل ما توثق في الفاحص، جرّبه على حزمة موجودة**: خليه يقارن الفصحى بالمصري كأنها
ترجمة جديدة. لازم يعدّي. لو عدّى، الجدول بتاع الحقول الحرفية فيه صح.

## الخط اللي اشتغل

54 دفعة بالتوازي (الحد 20 وكيل في نفس الوقت)، وكل وكيل:
1. بيقرا دليل الترجمة (اكتب واحد للخليجي زي اللي اتكتب للشامي)
2. بيترجم دفعته
3. **بيشغّل `check.mjs` على نفسه ويصلّح لحد ما ينضف** — دي اللي خلّت 54 دفعة
   تعدّي من غير جولة مراجعة ميكانيكية
4. بيرجّع تقرير: قرارات غير بديهية + أي حاجة غلط في **الأصل المصري**

## الفخاخ — كلها اتمسكت مرة قبل كده

1. **اسم القيمة مفتاح مش عنوان.** `VALUES[].name` بيتطابق في `SITS[].vals` و`VID[5]`
   و`goVal()`. **الحل: الوكلاء بينسخوه حرفيًا، و`assemble.mjs` بيعيد التسمية وينشرها
   في الأربعة مع بعض.** حط أسماء الخليجي في `RENAME_BY_DIALECT.gulf`.
2. **`secGo('ref','…')` بيموت بصمت.** بيلاقي القسم بمطابقة نص العنوان، ولو ما لقاش
   **ما بيعملش حاجة ولا بيقول خطأ**. الفصحى فقدت 4 وصلات من 24 كده.
   `assemble.mjs` بيصلّحها بالموضع دلوقتي — بس **تأكد بعد التجميع**.
3. **علامات `nav.toc2`** لو ما طابقتش، القسم بيقع في آخر مجموعة اتطابقت بهدوء.
   بتتحسب بالموضع دلوقتي، والبناء بيمسكها.
4. **حقول بيطابقها الكود تفضل حرفية:** `cat` · `group` · `k` (فطار/سناك/غدا/عشا) ·
   `tg` · `tag` · `lvl` · `evi.c` · `evi.src` · `evi.schools` · `src` ·
   `VID[0..5]` · `YTC[0,1,3]` · `RES[0,2,3]` · `ACT[0,3,6]` · «بدون تجهيز» ·
   `adapt[].kid` · `SCHOOLS[0,1]` · `DAY[0,1,3]`. الجدول كامل في `check.mjs`.
5. **`ST` و`STX` (604 قصة) تتنسخ حرفيًا** — `assemble.mjs` بيعملها لوحده.
6. **`{0}` و`{1}`** في نصوص الواجهة تفضل في مواضعها.
7. **القرآن والحديث حرفًا بحرف.**
8. **وحّد قرار واحد قبل ما تبدأ** واكتبه في الدليل: ضمير المخاطب الجمع، وإملاء
   ضمير الغايب. في الشامي الاتنين اتفرقوا بين الدفعات واتصلّح واحد بس منهم.

## قواعد الترجمة

خليجي طبيعي زي ما بيتقال في البيت، مش مصري بحروف خليجية ولا فصحى مخفّفة.
الأب بيتخاطب بـ«انت»، والطفل **مذكر دايمًا**، والأم والمس مؤنثات.
**ممنوع تغيير أي رقم ولا رابط ولا اسم جهة.** **ممنوع تحديد مكان القارئ** (أسماء
المصادر مستثناة). ممنوع backtick أو `${`.

**افحص فخاخ المفردات قبل ما تبدأ** زي ما اتعمل للشامي (هناك «لبن» = زبادي مش
حليب، وده كان أخطر فخ في الحزمة كلها).

## التحقق

```bash
node build/dialect/check.mjs          # كل الدفعات ✓
node build/dialect/assemble.mjs gulf
node build/dialect/policy.mjs gulf    # قواعد CLAUDE.md + تسرّب مصري
node build/build.js
node src/moderation.test.mjs
```

وبعدين **في المتصفح الحي** (`node build/dialect/serve.mjs 8788`):
- بدّل للخليجي وعُدّ: **72 قيمة · 50 موقف · 90 وجبة · 36 قسم مرجعي · 80 نشاط ·
  604 قصة · 81 فيديو · 15 خانة يوم**
- **توزيع أقسام المرجع لازم يبقى 7-8-3-7-6-3-2** وبصفر «fallback»
- افتح الفهرس واقفز لقسم · دوس شيب قيمة من موقف · شوفه على 375px

## اللي لسه مفتوح في الأصل المصري

في تقرير مفصّل اتسلّم مع جلسة الشامي (عيوب في `eg.json` والفصحى صلّحت ٦ منها
بصمت). أهمها للّي جاي:
- `ACT[26]` اسمه «يوجا الحيوانات» — مخالف لـ§1.3، والفصحى غيّرته والمصري لأ
- أغنية في `SITS[7]` (مرتين) و`SITS[35]`
- «الكورنيش» · «كمبوند» · «إسكندراني» · «هامور» · «ليلة الخميس» — تحديد مكان
  و`LOCATOR` في `build.js` ما بيمسكهمش
- ١٤ مصدر إماراتي في المرجع، منهم أرقام طوارئ وقانون بلد وغرامة بالدرهم
- `evi.schools` فيه ١٨ تسمية لـ٩ مدارس، و«انحياز الدليل» بقى نص ثابت رغم إن
  CLAUDE.md §9 بيمنع ده

**لو اتصلّحت في المصري قبل ما تبدأ الخليجي، الخليجي هيبدأ من نسخة نضيفة.**

---

بعد الخليجي فاضل: **مغربي** بنفس الخط بالظبط.
