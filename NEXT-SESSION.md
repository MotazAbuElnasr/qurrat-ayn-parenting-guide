# برومبت الجلسة الجاية — إضافة اللهجة الشامية

انسخ اللي بين السطرين ده وابعته كأول رسالة.

---

اقرا `CLAUDE.md` كامل الأول، وبعدين `build/build.js`، وبعدين افتح
`docs/content/msa.json` وشوف شكلها — دي النموذج اللي هتقلده.

**المطلوب: إضافة اللهجة الشامية (`sham`) كثالث لهجة، كاملة زي الفصحى — محتوى وواجهة.**

## الوضع الحالي

الموقع بقى **قشرة + حزم محتوى**:
- `docs/index.html` (97KB) — واجهة وكود بس، **مفيهاش محتوى**
- `docs/content/eg.json` — المصري، **وهو الأصل اللي كل لهجة بتترجم منه**
- `docs/content/msa.json` — الفصحى، كاملة
- كل حزمة فيها: `data` (12 مصفوفة) · `prose` (ref + food كـHTML) · `ui` (309 مفتاح) · `nav` (خرايط الصفحات)
- `src/index.js` فيه `/api/dialect` بيختار اللهجة من `cf.country`
- `node build/build.js` بيتحقق من كل حزمة وبيختم اللهجات المتاحة في القشرة **وفي الـWorker**

الشامي هيبقى الافتراضي في سوريا ولبنان والأردن وفلسطين (الخريطة موجودة في `src/index.js`).

## الخط اللي اشتغل في الفصحى

اعمل نفسه بالظبط:

1. **المحتوى** — 13 دفعة بالتوازي: القيم على 3 دفعات (24 لكل واحدة)، المواقف على 2 (25)،
   الوجبات، الأنشطة، الصغيرة (DAY+BOX+SCHOOLS+RES+YTC+VID)، القصص (ST+STX).
2. **النثر** — 4 دفعات: `prose.ref` على 3 بحدود `<h2 class="sec">`، و`prose.food` كاملة.
3. **الواجهة** — `ui` كاملة (309 مفتاح).
4. **كل دفعة وراها مراجع منفصل** مشافش الترجمة وهي بتتكتب، بيقارن حرف بحرف.

## الفخاخ اللي وقعت فيها — متقعش فيها تاني

1. **اسم القيمة مفتاح مش عنوان.** `VALUES[].name` بيتطابق في `SITS[].vals` و`VID[][5]`
   وفي `goVal('...')` جوه `prose.ref` و`prose.food`. لو ترجمته لازم تنقل المراجع كلها.
   **البناء بيمسك ده دلوقتي.**
2. **خرايط الصفحات بتطابق بنص العناوين.** `nav.toc2` و`nav.subpages` علاماتها لازم
   تطابق عناوين `prose` **بتاعة نفس الحزمة**. الطريقة: امشي بكل علامة مصرية للعنوان
   اللي بتطابقه في المصري، وخد **العنوان اللي في نفس الموضع** من الترجمة.
   لو علامة ملقتش مطابقة **مفيش خطأ بيطلع** — القسم بيقع في آخر مجموعة اتطابقت
   والصفحات بتتملي غلط بهدوء. **البناء بيمسك ده دلوقتي.**
   وخد بالك: علامتين (`اقترحلي وجبة` و`بنك الوجبات`) بتطابقوا عناوين بتتولد من القشرة
   مش من النثر — **دول يفضلوا زي ما هم في كل اللهجات**.
3. **قيم بيطابقها الكود تفضل حرفية**: `k` في الوجبات (فطار/غدا/عشا/سناك — لو ترجمتها
   الموقع بيقع)، `cat`، `group`، `tg`، `evi.c`، `grade`، `lvl`، وأسماء التبويبات في `TABS`.
4. **دفعة ممكن تزيد عنصر.** في الفصحى `VALUES-a` ترجم 25 بدل 24. **جمّع بالموضع
   (`from + i`) مش بالتتابع** — لو ألحقت وراء بعض تلت القيم بتتزح بواحد.
5. **`{0}` و`{1}` في نصوص الواجهة** لازم يفضلوا في مواضعهم — دي اللي بتخلي العدادات
   تحسب نفسها.
6. **القرآن والحديث يفضلوا حرفًا بحرف** — مايتعادش صياغتهم.

## قواعد الترجمة

شامي طبيعي زي ما بيتقال في البيت، مش مصري مكتوب بحروف شامية ولا فصحى مخفّفة.
الأب بيتخاطب بـ«انت»، والطفل **مذكر دايمًا**، والأم والمس مؤنثات.
**ممنوع تغيير أي رقم** (أعمار، جرامات، دقايق، نسب، `r = 0.26`، أحجام عينات، أرقام أحاديث)
**ولا أي رابط ولا اسم جهة** (NHS، AAP، CDC، WHO، USDA، AHA).
**ممنوع تحديد مكان القارئ** — لا دولة ولا عملة ولا رقم طوارئ ولا قانون بلد. أسماء المصادر مستثناة.
ممنوع backtick أو `${`.

## التحقق قبل النشر

```bash
node build/build.js          # لازم يعدي — بيتحقق من العدد والوصلات والعلامات
node src/moderation.test.mjs # لازم يعدي
```

وبعدين **في المتصفح الحي** (شغّل سيرفر بيقدّم `docs/` على الجذر وبيرد على `/api/dialect`):
- بدّل للشامي وعُدّ: 72 قيمة · 50 موقف · 90 وجبة · 36 قسم مرجعي
- **قارن توزيع أقسام المرجع بالمصري** — لازم يبقى 7-8-3-7-6-3-2
- افتح الفهرس واقفز لقسم — العنوان لازم يقع تحت الشريط اللاصق مش وراه
- شوفه على 375px
- دوس شيب قيمة من موقف — لازم يفتح القيمة فعلًا

بعد النشر: `curl -s https://qurrat-ain.aro.day/api/dialect` لازم يرجّع `sham` في `available`.

## ملاحظة

`wrangler dev` المحلي مش شغال (نسخته أقدم من `compatibility_date` في `wrangler.jsonc`).
استعمل سيرفر Node بسيط بيقدّم `docs/` وبيستوب `/api/dialect`، أو حدّث wrangler.

---

بعد الشامي فاضل: **خليجي** و**مغربي** بنفس الخط بالظبط.
